const fs=require("fs");let rawdata=fs.readFileSync("seo/Coverage.json"),json=JSON.parse(rawdata);const style=json.find(s=>"https://franckdemoute.fr/style.css"==s.url),usedCSS=style.ranges.map(s=>style.text.substring(s.start,s.end)).join("");fs.writeFileSync("seo/usedCSS.css",usedCSS);